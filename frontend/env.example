VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key
# API base URL
# - Local dev: keep empty (Vite proxy will forward /api -> http://localhost:5000) OR set to http://localhost:5000/api
# - Vercel: MUST be /api (relative), so it hits the Vercel Serverless function
VITE_API_URL=/api

